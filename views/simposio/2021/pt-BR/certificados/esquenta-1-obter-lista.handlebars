   {{> simposio/2021/pt-BR/headerInterno}}

  <section id="datas" style="background-color:rgb(221, 232, 243);">
    <div class="container">
      <div class="row">
        <div class="col-lg-8 mx-auto">
          <h2 style="margin-top: -6rem;">
            Certificados do Esquenta 1 EduComp <br>
            <small class="text-muted">(27 de março de 2021)</small>
          </h2> 
          <br>
          <h3>Obter certificado: </h3>
          <br>

          {{#each data}}
            {{#if registros}}
              <ul class="list-group">
                <li class="list-group-item disable" style="font-size: x-large;">{{nome}}</li>
                {{#each registros}}
                  <a href='/simposio/2021/certificados/esquenta/1/obter/arquivo'  target="_blank" class="list-group-item list-group-item-action active" style="margin-bottom: 2px;" onclick="arquivoCertificado()">{{funcao}}</a>
                {{/each}}
              </ul>
            {{/if}}
          {{/each}}

          {{!-- {{#if abartura_fechamento}}
            <p> <a href='/simposio/2021/certificados/esquenta/1/obter/arquivo/abertura-fechamento/{{email}}' target="_blank" class="btn btn-primary btn-lg" style="width: 250px;">Abertura fechamento</a> </p>
          {{/if}} --}}
        </div>
      </div>
    </div>

    <script>
      function arquivoCertificado() {
        console.log('Arquivo certificado')
        let url = '/simposio/2021/certificados/esquenta/1/obter/arquivo'
        let data = 
      }
    </script>

  </section>